# Stale Delta Fixture

This fixture protects the rule that `.delta/` contains only current, actionable
divergence between VRS intent and implementation or observed behavior. Completed
work should be pruned rather than retained as a reminder.
