# Requirements

## Context

The project records user-visible intent for an agent-operated tool.

## Requirements

- **REQ-R01 Cache implementation:** The system must use SQLite tables named
  `intent_nodes` and `intent_edges` to cache every parsed Markdown artifact.
